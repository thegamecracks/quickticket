# Postgres DEV — Deployment 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quickticket-postgres-dev
  namespace: quickticket
  labels:
    app: quickticket-postgres-dev
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: quickticket-postgres-dev
  template:
    metadata:
      labels:
        app: quickticket-postgres-dev
    spec:
      containers:
        - name: postgres
          image: postgres:18.4-alpine   # all PG majors get 5yr fixes; 18 = EOL 2030, keep dev/prod in sync
          ports:
            - containerPort: 5432
              name: pg
          env:
            - name: POSTGRES_USER
              value: "quickticket"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: quickticket-secret-dev
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: "quickticket"
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "quickticket", "-d", "quickticket"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "quickticket", "-d", "quickticket"]
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: pgdata
          persistentVolumeClaim:
            claimName: quickticket-pgdata-dev
---
# Postgres DEV - Service
apiVersion: v1
kind: Service
metadata:
  name: quickticket-postgres-dev
  namespace: quickticket
spec:
  selector:
    app: quickticket-postgres-dev
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: pg
---
# Postgres DEV - longhorn pvc
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quickticket-pgdata-dev
  namespace: quickticket
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
