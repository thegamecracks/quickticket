# Redis PROD — Deployment 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quickticket-redis-prod
  namespace: quickticket
  labels:
    app: quickticket-redis-prod
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: quickticket-redis-prod
  template:
    metadata:
      labels:
        app: quickticket-redis-prod
    spec:
      containers:
        - name: redis
          image: redis:8.2.9-alpine   # extended-support release (5yr fixes)
          ports:
            - containerPort: 6379
              name: redis
          command:
            - redis-server
            - --requirepass
            - $(REDIS_PASSWORD)
            - --appendonly
            - "yes"
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: quickticket-secret-prod
                  key: REDIS_PASSWORD
          volumeMounts:
            - name: redis-data
              mountPath: /data
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: redis-data
          persistentVolumeClaim:
            claimName: quickticket-redis-data-prod
---
# Redis PROD - Service
apiVersion: v1
kind: Service
metadata:
  name: quickticket-redis-prod
  namespace: quickticket
spec:
  selector:
    app: quickticket-redis-prod
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
---
# Redis PROD - longhorn pvc
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quickticket-redis-data-prod
  namespace: quickticket
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
